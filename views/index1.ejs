<!DOCTYPE html>
<html>
  <head>
    <script src="https://code.jquery.com/jquery-3.1.0.min.js"></script>
    <script src="https://d3js.org/d3.v4.min.js"></script>
    <style>
      .line {
        fill: none;
        stroke: steelblue;
        stroke-width: 2px;
      }
    </style>
  </head>
  <body>
  	<h1>You've got something...good job.</>
    <div id="viz"></div>
    <script type="text/javascript">
      $(document).ready(function () {
        var svg = d3.select("#viz")
            .append("svg")
            .attr("width", 400)
            .attr("height", 400);    

        // var data = [[10, 10], [20, 20], [30, 30]]; 

        // $.get("small_trot.dat", function (data2) {
        //   d3.dsvFormat(' ').parseRows(data2, function (data) {
            // console.log(data2);
            // for line in data2
            // return data2;
        var data = [
				  {secs:  22.480000, accel: 0.134766},
				  {secs:  22.500000, accel: 0.139160},
				  {secs:  22.520000, accel: 0.131348},
				  {secs:  22.550000, accel: 0.147949},
				  {secs:  22.570000, accel: 0.140381},
				  {secs:  22.600000, accel: 0.159424}
				  ]
				  console.log(data)
				  return data;
   
		var line = d3.line()
					    .x(function(d) { return x(d.secs); })
					    .y(function(d) { return y(d.accel); })

            // var line = d3.line();
            svg.append("path")
              .datum(data)
              .attr("class", "line")
              .attr("d", line);
              // console.log(data);

        //   d3.line().x(function (row) { return row.date;}).y(function (row) { return row.value; });
          // });
        // });
      });
    </script>
  </body>
</html>
